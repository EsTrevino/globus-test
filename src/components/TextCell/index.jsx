import React from "react";
import { TableTextContainer } from "./styled";

const TextCell = ({ children }) => {
  return <TableTextContainer>{children}</TableTextContainer>;
};

export default TextCell;
